# caddy-wit

ai grown wit based plugin wasm interface for caddy

basically there are a bunch of wit interfaces for guest wasm plugins

and theres like a caddy host that loads the wasm plugins

so the idea is you write plugins to implement the wit (sort of like implementing the go interfaces0

and these can be loaded like on demand, and if you pin hashes, even from like oci repos and stuff



